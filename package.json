{
  "name": "matecha_sekesi",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build_client": "mkdir -p server/client_dist && cd client && npm i && npm run build && cd .. && npm run start_server",
    "start_server": "cd server && npm i && node server.js"
  },
  "author": "joepi",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
  }
}
